1. After cloning the repo Michaela-docker/docker run : cd Michaela-docker/docker to get to the folder.

2. run:
cp prod1-cortex/.env.example prod1-cortex/.env
cp prod1-thehive/.env.example prod1-thehive/.env
cp prod1-misp/.env.example prod1-misp/.env

3. run: ./start-soc.sh

4.  TheHive = http://localhost:9000
    Cortex = http://localhost:9001
    MISP = https://localhost:9003

5.  TheHive --> username = admin@thehive.local  password = secret
    Cortex -->  username = admin@thehive.local  password = secret
    MISP -->    username = admin@admin.test     password = admin